# ──────────────────────────────────────────────────────────────────
# BeginSeq Studio — Dependencies
# ──────────────────────────────────────────────────────────────────
# Install with: pip install -r requirements.txt
# ──────────────────────────────────────────────────────────────────

# Web framework for the user interface
streamlit>=1.28.0

# Tabular data manipulation
pandas>=2.0.0

# Numerical operations (log, abs, etc.)
numpy>=1.24.0

# Plot visualization (volcano, MA, PCA, heatmap)
matplotlib>=3.7.0
seaborn>=0.12.0

# PCA and clustering for visualizations
scikit-learn>=1.3.0
scipy>=1.10.0

# Automatic label adjustment in plots
adjustText>=0.8

# Python implementation of the DESeq2 algorithm for
# RNA-seq differential expression analysis
pydeseq2>=0.4.0

# Batch effect correction (ComBat) for PCA
combat>=0.3.3

# HTTP requests for the GDC REST API (Dataset Creator)
requests>=2.28.0

# ── scRNA-seq Analysis ─────────────────────────────────────────
# Core single-cell analysis framework
scanpy>=1.9.0

# Annotated data container for single-cell data
anndata>=0.9.0

# Leiden community detection for clustering
leidenalg>=0.9.0
igraph>=0.10.0

# UMAP dimensionality reduction
umap-learn>=0.5.0

# Required by scanpy for HVG selection with seurat_v3 flavor
scikit-misc>=0.2.0

# Harmony batch effect correction for scRNA-seq integration
# v0.2+ uses PyTorch (GPU-accelerated); runs in an isolated subprocess
# to contain the RAM spike from the torch runtime.
harmonypy>=0.2.0

# ── Ambient RNA Removal (SoupX via R) ────────────────────────
# Python-to-R bridge for calling SoupX
rpy2>=3.5.0
